import values.*;
import show.*;

symbol fib;
overload #fib('n) = #fib('n - 1) + #fib('n - 2);
overload #fib(1) = 1;
overload #fib(0) = 0;
// segfault D:

main() {
  show(..#mapValues(#fib, 0, 1, 2, 3, 4, 5));
}
